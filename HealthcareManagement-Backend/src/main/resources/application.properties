server.port=8080

# Use TCP loopback explicitly and include common local-dev MySQL params.
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/healthcaresystem?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:learner@21Usha}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL55Dialect

security.basic.enable=false
security.ignored=/**
